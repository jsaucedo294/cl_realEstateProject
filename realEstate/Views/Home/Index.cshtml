@model List<realEstate.Models.REIProperty>
@{
    ViewBag.Title = "Home Page";
}


    <ul>
        @foreach (var property in Model)
        {
            <li>@property.UpdatedPropertyDetails.response.address.street</li>
        }
    </ul>
